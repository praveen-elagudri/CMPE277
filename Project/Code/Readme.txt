1. Open andriod studions and import the folder "Andriod APP"
2. Install nodejs on your machine
3. Open folder "backend component" and cmd "npm install && npm start"
4. Attach arduino uno to a usb port and Open folder "iot component" and cmd "npm install && npm start"

